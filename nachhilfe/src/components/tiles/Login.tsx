import React from 'react';
import { Avatar, VStack, Box, Input } from 'native-base';

export const Example = () => {
	return (
        <>
            <Input isDisabled={true} m={3} />
            <Input isDisabled={true} m={3} />
        </>
	);
};
